# Domain Docs

This is a single-context repository. Read the root `CONTEXT.md` before work
that uses project domain language, and use its canonical terms in specifications
and issue titles. Check `docs/adr/` for relevant decisions when that directory
exists.
