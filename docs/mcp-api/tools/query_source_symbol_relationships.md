<!-- Generated by `reforger_language_server mcp-api-bundle`. Do not edit manually. -->
# `query_source_symbol_relationships`

[Back to the MCP API router](../../mcp-api.md)

Query exact inheritance, modded-class, and method-override relationships across the immutable workspace and selected Game Data scope. Copy anchorSource and symbolRef from semantic search; identities and relationships remain Rust-owned.

### Annotations

```json
{
  "title": "Query source symbol relationships",
  "readOnlyHint": true,
  "openWorldHint": false
}
```

### Input schema

```json
{
  "$defs": {
    "SourceAuthority": {
      "enum": [
        "workspace",
        "gameData"
      ],
      "type": "string"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "addonGuids": {
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "anchorSource": {
      "$ref": "#/$defs/SourceAuthority"
    },
    "cursor": {
      "maxLength": 4096,
      "type": [
        "string",
        "null"
      ]
    },
    "depth": {
      "type": "string"
    },
    "includeWorkspace": {
      "type": "boolean"
    },
    "kinds": {
      "items": {
        "type": "string"
      },
      "type": [
        "array",
        "null"
      ]
    },
    "limit": {
      "minimum": 0,
      "type": [
        "integer",
        "null"
      ]
    },
    "relationshipKinds": {
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "type": "array"
    },
    "symbolRef": {
      "maxLength": 2048,
      "minLength": 1,
      "type": "string"
    }
  },
  "required": [
    "anchorSource",
    "symbolRef",
    "includeWorkspace",
    "addonGuids",
    "relationshipKinds",
    "depth"
  ],
  "type": "object"
}
```

### Output schema

```json
{
  "$defs": {
    "ReadSourceInput": {
      "properties": {
        "addonGuid": {
          "type": [
            "string",
            "null"
          ]
        },
        "catalogueRevision": {
          "type": "string"
        },
        "relativePath": {
          "type": "string"
        },
        "startLine": {
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "catalogueRevision",
        "relativePath",
        "startLine"
      ],
      "type": "object"
    },
    "SourceAuthority": {
      "enum": [
        "workspace",
        "gameData"
      ],
      "type": "string"
    },
    "SourceLineRange": {
      "properties": {
        "endLine": {
          "minimum": 0,
          "type": "integer"
        },
        "startLine": {
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "startLine",
        "endLine"
      ],
      "type": "object"
    },
    "SourceRelationshipHit": {
      "properties": {
        "addonGuid": {
          "type": [
            "string",
            "null"
          ]
        },
        "addonLabel": {
          "type": [
            "string",
            "null"
          ]
        },
        "declarationRange": {
          "$ref": "#/$defs/SourceLineRange"
        },
        "distance": {
          "minimum": 0,
          "type": "integer"
        },
        "evidence": {
          "type": "string"
        },
        "kind": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "qualifiedName": {
          "type": "string"
        },
        "readSourceInput": {
          "$ref": "#/$defs/ReadSourceInput"
        },
        "relationshipKind": {
          "type": "string"
        },
        "relativePath": {
          "type": "string"
        },
        "selectionRange": {
          "$ref": "#/$defs/SourceLineRange"
        },
        "signature": {
          "type": "string"
        },
        "source": {
          "$ref": "#/$defs/SourceAuthority"
        },
        "sourceCategory": {
          "type": "string"
        },
        "symbolRef": {
          "type": "string"
        }
      },
      "required": [
        "source",
        "symbolRef",
        "name",
        "kind",
        "qualifiedName",
        "signature",
        "sourceCategory",
        "relativePath",
        "relationshipKind",
        "distance",
        "evidence",
        "declarationRange",
        "selectionRange",
        "readSourceInput"
      ],
      "type": "object"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
    "anchorSource": {
      "$ref": "#/$defs/SourceAuthority"
    },
    "depth": {
      "type": "string"
    },
    "nextCursor": {
      "type": [
        "string",
        "null"
      ]
    },
    "relationshipKinds": {
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "relationshipRevision": {
      "type": "string"
    },
    "results": {
      "items": {
        "$ref": "#/$defs/SourceRelationshipHit"
      },
      "type": "array"
    },
    "returned": {
      "minimum": 0,
      "type": "integer"
    },
    "targetSymbolRef": {
      "type": "string"
    },
    "total": {
      "minimum": 0,
      "type": "integer"
    },
    "truncated": {
      "type": "boolean"
    },
    "warnings": {
      "items": {
        "type": "string"
      },
      "type": "array"
    }
  },
  "required": [
    "relationshipRevision",
    "anchorSource",
    "targetSymbolRef",
    "relationshipKinds",
    "depth",
    "returned",
    "total",
    "truncated",
    "warnings",
    "results"
  ],
  "type": "object"
}
```

### Limits and semantics

- Copy `anchorSource` and `symbolRef` from one exact Workspace or Game Data semantic-search result. The opaque reference is never decoded by the caller.
- `relationshipKinds` accepts `direct`, `directBase`, `derivedType`, `moddedExtension`, `overriddenDeclaration`, and `override`; `depth` is `one` or `all`. `kinds` filters emitted symbol kinds without changing relationship resolution.
- `includeWorkspace` and `addonGuids` control emitted declarations. Resolution still uses the complete captured semantic graph, so an excluded intermediate does not break a proven edge.
- Results are deterministic, capped at 5,000 traversed declarations and 100 records per page, cancellable, and subject to the ready-operation five-second deadline. Cycles and ambiguous omitted edges are reported as warnings.
- The opaque cursor is bound to both source revisions, exact anchor, output scope, relationship kinds, result kinds, and depth.

### Stable failures

- `invalid_source_relationship_request`: correct the relationship kinds, depth, or filters.
- `invalid_relationship_anchor` or `stale_relationship_anchor`: return to broad semantic discovery and select a current exact declaration.
- `invalid_relationship_cursor` or `stale_relationship_cursor`: restart paging from page one.
- `source_relationships_unavailable`: restore the anchor source and select a current anchor.
- `request_cancelled` or `deadline_exceeded`: retry or narrow the selected relationships.

### Result handoff

Preserve each result's source authority and add-on GUID. Copy `readSourceInput` unchanged to `read_workspace_source` or `read_game_data_source` according to `source`.
