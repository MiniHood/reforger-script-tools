<!-- Generated by `reforger_language_server mcp-api-bundle`. Do not edit manually. -->
# `research_game_data`

[Back to the MCP API router](../../mcp-api.md)

Resolve one exact identifier or natural-language Game Data declaration need in a single bounded call. Returns one compact primary declaration, at most two compact alternatives, and at most five direct members that match the query. It does not include source text, examples, or relationships. Start here when the declaration name is uncertain; use the returned source handoff or narrow tools only when more evidence is materially required.

### Annotations

```json
{
  "title": "Research Game Data",
  "readOnlyHint": true,
  "openWorldHint": false
}
```

### Input schema

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "addonGuids": {
      "description": "Canonical loaded add-on GUIDs returned by game_data_status. Omit to search every available add-on; an empty list is invalid.",
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "type": [
        "array",
        "null"
      ]
    },
    "query": {
      "maxLength": 256,
      "minLength": 1,
      "type": "string"
    }
  },
  "required": [
    "query"
  ],
  "type": "object"
}
```

### Output schema

```json
{
  "$defs": {
    "InspectionConditionalContext": {
      "properties": {
        "condition": {
          "type": [
            "string",
            "null"
          ]
        },
        "kind": {
          "type": "string"
        }
      },
      "required": [
        "kind"
      ],
      "type": "object"
    },
    "IntentAlternative": {
      "properties": {
        "kind": {
          "type": "string"
        },
        "matchedTerms": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "qualifiedName": {
          "type": "string"
        },
        "signature": {
          "type": "string"
        },
        "symbolRef": {
          "type": "string"
        }
      },
      "required": [
        "symbolRef",
        "qualifiedName",
        "kind",
        "signature",
        "matchedTerms"
      ],
      "type": "object"
    },
    "IntentFollowUp": {
      "enum": [
        "none",
        "refineQuery"
      ],
      "type": "string"
    },
    "IntentMember": {
      "properties": {
        "kind": {
          "type": "string"
        },
        "matchedTerms": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "name": {
          "type": "string"
        },
        "signature": {
          "type": "string"
        },
        "symbolRef": {
          "type": "string"
        }
      },
      "required": [
        "symbolRef",
        "name",
        "kind",
        "signature",
        "matchedTerms"
      ],
      "type": "object"
    },
    "IntentStatus": {
      "enum": [
        "resolved",
        "ambiguous",
        "notFound"
      ],
      "type": "string"
    },
    "IntentSymbol": {
      "properties": {
        "attributes": {
          "items": {
            "type": "string"
          },
          "maxItems": 16,
          "type": "array"
        },
        "baseType": {
          "type": [
            "string",
            "null"
          ]
        },
        "callableForm": {
          "type": [
            "string",
            "null"
          ]
        },
        "conditionalContext": {
          "items": {
            "$ref": "#/$defs/InspectionConditionalContext"
          },
          "maxItems": 8,
          "type": "array"
        },
        "declarationRange": {
          "$ref": "#/$defs/SourceLineRange"
        },
        "defaultValue": {
          "type": [
            "string",
            "null"
          ]
        },
        "documentationSummary": {
          "type": [
            "string",
            "null"
          ]
        },
        "enumValue": {
          "type": [
            "string",
            "null"
          ]
        },
        "kind": {
          "type": "string"
        },
        "matchedTerms": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "modifiers": {
          "items": {
            "type": "string"
          },
          "maxItems": 8,
          "type": "array"
        },
        "qualifiedName": {
          "type": "string"
        },
        "readSourceInput": {
          "$ref": "#/$defs/ReadSourceInput"
        },
        "relativePath": {
          "type": "string"
        },
        "relevantMembers": {
          "items": {
            "$ref": "#/$defs/IntentMember"
          },
          "maxItems": 5,
          "type": "array"
        },
        "returnType": {
          "type": [
            "string",
            "null"
          ]
        },
        "signature": {
          "type": "string"
        },
        "sourceCategory": {
          "type": "string"
        },
        "symbolRef": {
          "type": "string"
        },
        "type": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "symbolRef",
        "qualifiedName",
        "kind",
        "signature",
        "matchedTerms",
        "modifiers",
        "attributes",
        "conditionalContext",
        "sourceCategory",
        "relativePath",
        "declarationRange",
        "readSourceInput",
        "relevantMembers"
      ],
      "type": "object"
    },
    "ReadSourceInput": {
      "properties": {
        "addonGuid": {
          "type": [
            "string",
            "null"
          ]
        },
        "catalogueRevision": {
          "type": "string"
        },
        "relativePath": {
          "type": "string"
        },
        "startLine": {
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "catalogueRevision",
        "relativePath",
        "startLine"
      ],
      "type": "object"
    },
    "SourceLineRange": {
      "properties": {
        "endLine": {
          "minimum": 0,
          "type": "integer"
        },
        "startLine": {
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "startLine",
        "endLine"
      ],
      "type": "object"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
    "alternatives": {
      "items": {
        "$ref": "#/$defs/IntentAlternative"
      },
      "maxItems": 2,
      "type": "array"
    },
    "catalogueRevision": {
      "type": "string"
    },
    "followUp": {
      "$ref": "#/$defs/IntentFollowUp"
    },
    "primary": {
      "anyOf": [
        {
          "$ref": "#/$defs/IntentSymbol"
        },
        {
          "type": "null"
        }
      ]
    },
    "query": {
      "type": "string"
    },
    "status": {
      "$ref": "#/$defs/IntentStatus"
    }
  },
  "required": [
    "status",
    "catalogueRevision",
    "query",
    "alternatives",
    "followUp"
  ],
  "type": "object"
}
```

### Bounds and matching

- `query` is required, normalized whitespace, and limited to 256 characters. It may be one exact identifier or a natural-language declaration need.
- Identifier words are split across underscores and case transitions. Name, owner, signature, type, and documentation terms contribute to deterministic ranking.
- One primary declaration is returned with at most two compact alternatives and at most five matching direct members. There is no paging or caller-controlled result limit.
- Source bodies, examples, and relationships are not included. `resolved` with `followUp: none` is a stop signal; `ambiguous` and `notFound` use `refineQuery`.
- The operation is read-only, revision-bound, cancellable, and subject to the ready-operation five-second deadline.

### Stable failures

- `invalid_arguments`: correct the query or add-on GUID scope.
- `game_data_unavailable`: call `game_data_status` and correct configuration.
- `deadline_exceeded`: retry or make the intent more specific.

### Result handoff

Stop when `followUp` is `none`. Copy the primary declaration's `readSourceInput` to `read_game_data_source` only when verbatim source is materially required.
