<!-- Generated by `reforger_language_server mcp-api-bundle`. Do not edit manually. -->
# `search_reforger`

[Back to the MCP API router](../../mcp-api.md)

Search Game Data declarations, workspace declarations, and Official Wiki passages. Returns one labeled hit per source, with exact handoffs only when resolved.

### Search scope

One query may select any combination of `gameData`, `workspace`, and `officialWiki`; omitting `sources` selects all three. Each selected authority contributes at most one compact result. Game Data and workspace matching is declaration-aware, while Official Wiki matching returns a passage. This tool does not scan literal source text or discover resources; those specialist operations remain available in the `all` profile.

### Annotations

```json
{
  "title": "Search Reforger",
  "readOnlyHint": true,
  "openWorldHint": false
}
```

### Input schema

```json
{
  "$defs": {
    "ReforgerSearchSource": {
      "enum": [
        "gameData",
        "workspace",
        "officialWiki"
      ],
      "type": "string"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "query": {
      "maxLength": 256,
      "minLength": 1,
      "type": "string"
    },
    "sources": {
      "items": {
        "$ref": "#/$defs/ReforgerSearchSource"
      },
      "maxItems": 3,
      "minItems": 1,
      "type": [
        "array",
        "null"
      ]
    }
  },
  "required": [
    "query"
  ],
  "type": "object"
}
```

### Output schema

```json
{
  "$defs": {
    "ReforgerSearchAlternative": {
      "properties": {
        "description": {
          "type": "string"
        },
        "kind": {
          "type": "string"
        },
        "title": {
          "type": "string"
        }
      },
      "required": [
        "kind",
        "title",
        "description"
      ],
      "type": "object"
    },
    "ReforgerSearchHandoff": {
      "properties": {
        "arguments": true,
        "tool": {
          "type": "string"
        }
      },
      "required": [
        "tool",
        "arguments"
      ],
      "type": "object"
    },
    "ReforgerSearchHit": {
      "properties": {
        "alternatives": {
          "items": {
            "$ref": "#/$defs/ReforgerSearchAlternative"
          },
          "type": "array"
        },
        "description": {
          "type": "string"
        },
        "inspect": {
          "anyOf": [
            {
              "$ref": "#/$defs/ReforgerSearchHandoff"
            },
            {
              "type": "null"
            }
          ]
        },
        "kind": {
          "type": "string"
        },
        "location": {
          "type": [
            "string",
            "null"
          ]
        },
        "read": {
          "anyOf": [
            {
              "$ref": "#/$defs/ReforgerSearchHandoff"
            },
            {
              "type": "null"
            }
          ]
        },
        "relevantMember": {
          "anyOf": [
            {
              "$ref": "#/$defs/ReforgerSearchRelatedSymbol"
            },
            {
              "type": "null"
            }
          ]
        },
        "source": {
          "$ref": "#/$defs/ReforgerSearchSource"
        },
        "status": {
          "$ref": "#/$defs/ReforgerSearchStatus"
        },
        "symbolRef": {
          "type": [
            "string",
            "null"
          ]
        },
        "title": {
          "type": "string"
        }
      },
      "required": [
        "source",
        "status",
        "kind",
        "title",
        "description",
        "alternatives"
      ],
      "type": "object"
    },
    "ReforgerSearchRelatedSymbol": {
      "properties": {
        "description": {
          "type": "string"
        },
        "inspect": {
          "$ref": "#/$defs/ReforgerSearchHandoff"
        },
        "kind": {
          "type": "string"
        },
        "symbolRef": {
          "type": "string"
        },
        "title": {
          "type": "string"
        }
      },
      "required": [
        "kind",
        "title",
        "description",
        "symbolRef",
        "inspect"
      ],
      "type": "object"
    },
    "ReforgerSearchSource": {
      "enum": [
        "gameData",
        "workspace",
        "officialWiki"
      ],
      "type": "string"
    },
    "ReforgerSearchSourceReport": {
      "properties": {
        "description": {
          "type": "string"
        },
        "source": {
          "$ref": "#/$defs/ReforgerSearchSource"
        },
        "status": {
          "$ref": "#/$defs/ReforgerSearchStatus"
        }
      },
      "required": [
        "source",
        "status",
        "description"
      ],
      "type": "object"
    },
    "ReforgerSearchStatus": {
      "enum": [
        "resolved",
        "ambiguous",
        "notFound",
        "unavailable"
      ],
      "type": "string"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
    "query": {
      "type": "string"
    },
    "results": {
      "items": {
        "$ref": "#/$defs/ReforgerSearchHit"
      },
      "type": "array"
    },
    "sources": {
      "items": {
        "$ref": "#/$defs/ReforgerSearchSourceReport"
      },
      "type": "array"
    }
  },
  "required": [
    "query",
    "results",
    "sources"
  ],
  "type": "object"
}
```

### Failure model

Invalid schemas and unavailable tools are MCP protocol errors. Valid calls return either their typed evidence result or the stable structured error owned by the selected Game Data, workspace, or Official Wiki authority. This read-only tool does not return Workbench operation phases or integration-log references.
