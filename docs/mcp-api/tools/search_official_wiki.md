<!-- Generated by `reforger_language_server mcp-api-bundle`. Do not edit manually. -->
# `search_official_wiki`

[Back to the MCP API router](../../mcp-api.md)

Search validated packaged Official Wiki Markdown directly for deterministic, section-local passages. Results carry canonical source URLs, exact line ranges, and copy-ready read inputs; this never searches wiki-index.md or exposes an installed path.

### Annotations

```json
{
  "title": "Search Official Wiki",
  "readOnlyHint": true,
  "openWorldHint": false
}
```

### Input schema

```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "cursor": {
      "maxLength": 2048,
      "type": [
        "string",
        "null"
      ]
    },
    "limit": {
      "minimum": 0,
      "type": [
        "integer",
        "null"
      ]
    },
    "pathPrefix": {
      "maxLength": 2048,
      "minLength": 1,
      "type": [
        "string",
        "null"
      ]
    },
    "query": {
      "maxLength": 256,
      "minLength": 1,
      "type": "string"
    }
  },
  "required": [
    "query"
  ],
  "type": "object"
}
```

### Output schema

```json
{
  "$defs": {
    "OfficialWikiMatchKind": {
      "enum": [
        "exactTitle",
        "titlePhrase",
        "path",
        "heading",
        "body"
      ],
      "type": "string"
    },
    "OfficialWikiReadInput": {
      "properties": {
        "corpusRevision": {
          "type": "string"
        },
        "lineCount": {
          "minimum": 0,
          "type": "integer"
        },
        "relativePath": {
          "type": "string"
        },
        "startLine": {
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "corpusRevision",
        "relativePath",
        "startLine",
        "lineCount"
      ],
      "type": "object"
    },
    "OfficialWikiSearchFilters": {
      "properties": {
        "limit": {
          "minimum": 0,
          "type": "integer"
        },
        "pathPrefix": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "limit"
      ],
      "type": "object"
    },
    "OfficialWikiSearchHit": {
      "properties": {
        "endLine": {
          "minimum": 0,
          "type": "integer"
        },
        "excerpt": {
          "type": "string"
        },
        "excerptEndLine": {
          "minimum": 0,
          "type": "integer"
        },
        "excerptStartLine": {
          "minimum": 0,
          "type": "integer"
        },
        "heading": {
          "type": "string"
        },
        "matchKind": {
          "$ref": "#/$defs/OfficialWikiMatchKind"
        },
        "matchedFields": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "matchedLine": {
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "readInput": {
          "$ref": "#/$defs/OfficialWikiReadInput"
        },
        "relativePath": {
          "type": "string"
        },
        "sourceUrl": {
          "type": "string"
        },
        "startLine": {
          "minimum": 0,
          "type": "integer"
        },
        "title": {
          "type": "string"
        }
      },
      "required": [
        "relativePath",
        "title",
        "heading",
        "startLine",
        "endLine",
        "excerpt",
        "excerptStartLine",
        "excerptEndLine",
        "sourceUrl",
        "matchedFields",
        "matchKind",
        "readInput"
      ],
      "type": "object"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
    "appliedFilters": {
      "$ref": "#/$defs/OfficialWikiSearchFilters"
    },
    "corpusRevision": {
      "type": "string"
    },
    "nextCursor": {
      "type": [
        "string",
        "null"
      ]
    },
    "query": {
      "type": "string"
    },
    "results": {
      "items": {
        "$ref": "#/$defs/OfficialWikiSearchHit"
      },
      "type": "array"
    },
    "returned": {
      "minimum": 0,
      "type": "integer"
    },
    "source": {
      "type": "string"
    },
    "total": {
      "minimum": 0,
      "type": "integer"
    }
  },
  "required": [
    "source",
    "corpusRevision",
    "query",
    "appliedFilters",
    "returned",
    "total",
    "results"
  ],
  "type": "object"
}
```

### Limits, matching, and recovery

- `query` is required, normalized whitespace, and limited to 256 characters. `pathPrefix` is an optional safe logical subtree filter.
- `limit` defaults to 20 and clamps visibly to 1 through 100; cursors are opaque, revision-bound, and limited to 2 KiB.
- Every normalized query term must match within the same page's logical path, title, or one heading section (heading or body). At most one hit is returned per matching section.
- Fixed ranking favors exact title/phrase, path, heading, then body matches; logical path and start line break ties. No numeric relevance score is returned.
- Results are direct UTF-8 Markdown projections, exclude `wiki-index.md`, verify validation hashes, and remain below 256 KiB. A changed page returns `official_wiki_changed`.
- Excerpts have at most 12 complete lines and 4 KiB; `readInput` can be copied to `read_official_wiki` when that tool is available.

### Stable failures

- `invalid_query`, `invalid_filter`, and `invalid_cursor`: correct the supplied arguments and retry.
- `stale_cursor`: repeat the same search without the cursor.
- `official_wiki_unavailable`: call `official_wiki_status`.
- `official_wiki_changed`: restart or reconfigure the MCP process against the current installed extension.

### Example call

```json
{"name":"search_official_wiki","arguments":{"query":"Game Master","pathPrefix":"Modding/","limit":20}}
```

### Result handoff

Use a hit's `readInput` unchanged with `read_official_wiki`; preserve `corpusRevision` and the exact logical range.
